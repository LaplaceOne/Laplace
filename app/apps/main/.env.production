# Production build config (committed — public URLs, not secrets). Vite loads this automatically for
# `vite build`/`npm run build`; `npm run dev` is unaffected. Used by the Cloudflare Pages deploy.
VITE_CLUSTER=devnet
VITE_INDEXER_URL=https://indexer.laplace.one
