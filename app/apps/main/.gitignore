# Root .gitignore ignores .env and .env.* — re-include the committed production deploy config
# (public, non-secret URLs). Local env files (.env, .env.local, .env.development) stay ignored.
!.env.production
