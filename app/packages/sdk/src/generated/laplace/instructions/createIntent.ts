/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU64Decoder,
  getU64Encoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type Codec,
  type Decoder,
  type Encoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import { LAPLACE_PROGRAM_ADDRESS } from "../programs";
import {
  getEscrowAssetDecoder,
  getEscrowAssetEncoder,
  type EscrowAsset,
  type EscrowAssetArgs,
} from "../types";

export const CREATE_INTENT_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([
  216, 214, 79, 121, 23, 194, 96, 104,
]);

export function getCreateIntentDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    CREATE_INTENT_DISCRIMINATOR,
  );
}

export type CreateIntentInstruction<
  TProgram extends string = typeof LAPLACE_PROGRAM_ADDRESS,
  TAccountMaker extends string | AccountMeta<string> = string,
  TAccountIntent extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends string | AccountMeta<string> =
    "11111111111111111111111111111111",
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountMaker extends string
        ? WritableSignerAccount<TAccountMaker> &
            AccountSignerMeta<TAccountMaker>
        : TAccountMaker,
      TAccountIntent extends string
        ? WritableAccount<TAccountIntent>
        : TAccountIntent,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type CreateIntentInstructionData = {
  discriminator: ReadonlyUint8Array;
  id: ReadonlyUint8Array;
  receiver: Address;
  refundRecipient: Address;
  criterionProgram: Address;
  asset: EscrowAsset;
  amount: bigint;
  expirySlot: bigint;
  criterionDataHash: ReadonlyUint8Array;
};

export type CreateIntentInstructionDataArgs = {
  id: ReadonlyUint8Array;
  receiver: Address;
  refundRecipient: Address;
  criterionProgram: Address;
  asset: EscrowAssetArgs;
  amount: number | bigint;
  expirySlot: number | bigint;
  criterionDataHash: ReadonlyUint8Array;
};

export function getCreateIntentInstructionDataEncoder(): Encoder<CreateIntentInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["id", fixEncoderSize(getBytesEncoder(), 32)],
      ["receiver", getAddressEncoder()],
      ["refundRecipient", getAddressEncoder()],
      ["criterionProgram", getAddressEncoder()],
      ["asset", getEscrowAssetEncoder()],
      ["amount", getU64Encoder()],
      ["expirySlot", getU64Encoder()],
      ["criterionDataHash", fixEncoderSize(getBytesEncoder(), 32)],
    ]),
    (value) => ({ ...value, discriminator: CREATE_INTENT_DISCRIMINATOR }),
  );
}

export function getCreateIntentInstructionDataDecoder(): Decoder<CreateIntentInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["id", fixDecoderSize(getBytesDecoder(), 32)],
    ["receiver", getAddressDecoder()],
    ["refundRecipient", getAddressDecoder()],
    ["criterionProgram", getAddressDecoder()],
    ["asset", getEscrowAssetDecoder()],
    ["amount", getU64Decoder()],
    ["expirySlot", getU64Decoder()],
    ["criterionDataHash", fixDecoderSize(getBytesDecoder(), 32)],
  ]);
}

export function getCreateIntentInstructionDataCodec(): Codec<
  CreateIntentInstructionDataArgs,
  CreateIntentInstructionData
> {
  return combineCodec(
    getCreateIntentInstructionDataEncoder(),
    getCreateIntentInstructionDataDecoder(),
  );
}

export type CreateIntentInput<
  TAccountMaker extends string = string,
  TAccountIntent extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  maker: TransactionSigner<TAccountMaker>;
  intent: Address<TAccountIntent>;
  systemProgram?: Address<TAccountSystemProgram>;
  id: CreateIntentInstructionDataArgs["id"];
  receiver: CreateIntentInstructionDataArgs["receiver"];
  refundRecipient: CreateIntentInstructionDataArgs["refundRecipient"];
  criterionProgram: CreateIntentInstructionDataArgs["criterionProgram"];
  asset: CreateIntentInstructionDataArgs["asset"];
  amount: CreateIntentInstructionDataArgs["amount"];
  expirySlot: CreateIntentInstructionDataArgs["expirySlot"];
  criterionDataHash: CreateIntentInstructionDataArgs["criterionDataHash"];
};

export function getCreateIntentInstruction<
  TAccountMaker extends string,
  TAccountIntent extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof LAPLACE_PROGRAM_ADDRESS,
>(
  input: CreateIntentInput<
    TAccountMaker,
    TAccountIntent,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): CreateIntentInstruction<
  TProgramAddress,
  TAccountMaker,
  TAccountIntent,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress = config?.programAddress ?? LAPLACE_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    maker: { value: input.maker ?? null, isWritable: true },
    intent: { value: input.intent ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("maker", accounts.maker),
      getAccountMeta("intent", accounts.intent),
      getAccountMeta("systemProgram", accounts.systemProgram),
    ],
    data: getCreateIntentInstructionDataEncoder().encode(
      args as CreateIntentInstructionDataArgs,
    ),
    programAddress,
  } as CreateIntentInstruction<
    TProgramAddress,
    TAccountMaker,
    TAccountIntent,
    TAccountSystemProgram
  >);
}

export type ParsedCreateIntentInstruction<
  TProgram extends string = typeof LAPLACE_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    maker: TAccountMetas[0];
    intent: TAccountMetas[1];
    systemProgram: TAccountMetas[2];
  };
  data: CreateIntentInstructionData;
};

export function parseCreateIntentInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedCreateIntentInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 3) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 3,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      maker: getNextAccount(),
      intent: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getCreateIntentInstructionDataDecoder().decode(instruction.data),
  };
}
