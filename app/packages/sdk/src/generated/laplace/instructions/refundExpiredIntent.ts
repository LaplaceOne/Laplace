/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyUint8Array,
  type WritableAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import { LAPLACE_PROGRAM_ADDRESS } from "../programs";

export const REFUND_EXPIRED_INTENT_DISCRIMINATOR: ReadonlyUint8Array =
  new Uint8Array([132, 39, 201, 69, 81, 208, 253, 235]);

export function getRefundExpiredIntentDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    REFUND_EXPIRED_INTENT_DISCRIMINATOR,
  );
}

export type RefundExpiredIntentInstruction<
  TProgram extends string = typeof LAPLACE_PROGRAM_ADDRESS,
  TAccountIntent extends string | AccountMeta<string> = string,
  TAccountRefundRecipient extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountIntent extends string
        ? WritableAccount<TAccountIntent>
        : TAccountIntent,
      TAccountRefundRecipient extends string
        ? WritableAccount<TAccountRefundRecipient>
        : TAccountRefundRecipient,
      ...TRemainingAccounts,
    ]
  >;

export type RefundExpiredIntentInstructionData = {
  discriminator: ReadonlyUint8Array;
};

export type RefundExpiredIntentInstructionDataArgs = {};

export function getRefundExpiredIntentInstructionDataEncoder(): FixedSizeEncoder<RefundExpiredIntentInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([["discriminator", fixEncoderSize(getBytesEncoder(), 8)]]),
    (value) => ({
      ...value,
      discriminator: REFUND_EXPIRED_INTENT_DISCRIMINATOR,
    }),
  );
}

export function getRefundExpiredIntentInstructionDataDecoder(): FixedSizeDecoder<RefundExpiredIntentInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
  ]);
}

export function getRefundExpiredIntentInstructionDataCodec(): FixedSizeCodec<
  RefundExpiredIntentInstructionDataArgs,
  RefundExpiredIntentInstructionData
> {
  return combineCodec(
    getRefundExpiredIntentInstructionDataEncoder(),
    getRefundExpiredIntentInstructionDataDecoder(),
  );
}

export type RefundExpiredIntentInput<
  TAccountIntent extends string = string,
  TAccountRefundRecipient extends string = string,
> = {
  intent: Address<TAccountIntent>;
  refundRecipient: Address<TAccountRefundRecipient>;
};

export function getRefundExpiredIntentInstruction<
  TAccountIntent extends string,
  TAccountRefundRecipient extends string,
  TProgramAddress extends Address = typeof LAPLACE_PROGRAM_ADDRESS,
>(
  input: RefundExpiredIntentInput<TAccountIntent, TAccountRefundRecipient>,
  config?: { programAddress?: TProgramAddress },
): RefundExpiredIntentInstruction<
  TProgramAddress,
  TAccountIntent,
  TAccountRefundRecipient
> {
  // Program address.
  const programAddress = config?.programAddress ?? LAPLACE_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    intent: { value: input.intent ?? null, isWritable: true },
    refundRecipient: { value: input.refundRecipient ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("intent", accounts.intent),
      getAccountMeta("refundRecipient", accounts.refundRecipient),
    ],
    data: getRefundExpiredIntentInstructionDataEncoder().encode({}),
    programAddress,
  } as RefundExpiredIntentInstruction<
    TProgramAddress,
    TAccountIntent,
    TAccountRefundRecipient
  >);
}

export type ParsedRefundExpiredIntentInstruction<
  TProgram extends string = typeof LAPLACE_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    intent: TAccountMetas[0];
    refundRecipient: TAccountMetas[1];
  };
  data: RefundExpiredIntentInstructionData;
};

export function parseRefundExpiredIntentInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedRefundExpiredIntentInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 2) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 2,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: { intent: getNextAccount(), refundRecipient: getNextAccount() },
    data: getRefundExpiredIntentInstructionDataDecoder().decode(
      instruction.data,
    ),
  };
}
