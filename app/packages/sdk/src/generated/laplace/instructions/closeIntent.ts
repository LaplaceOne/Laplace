/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import { LAPLACE_PROGRAM_ADDRESS } from "../programs";

export const CLOSE_INTENT_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([
  112, 245, 154, 249, 57, 126, 54, 122,
]);

export function getCloseIntentDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    CLOSE_INTENT_DISCRIMINATOR,
  );
}

export type CloseIntentInstruction<
  TProgram extends string = typeof LAPLACE_PROGRAM_ADDRESS,
  TAccountIntent extends string | AccountMeta<string> = string,
  TAccountMaker extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountIntent extends string
        ? WritableAccount<TAccountIntent>
        : TAccountIntent,
      TAccountMaker extends string
        ? WritableSignerAccount<TAccountMaker> &
            AccountSignerMeta<TAccountMaker>
        : TAccountMaker,
      ...TRemainingAccounts,
    ]
  >;

export type CloseIntentInstructionData = { discriminator: ReadonlyUint8Array };

export type CloseIntentInstructionDataArgs = {};

export function getCloseIntentInstructionDataEncoder(): FixedSizeEncoder<CloseIntentInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([["discriminator", fixEncoderSize(getBytesEncoder(), 8)]]),
    (value) => ({ ...value, discriminator: CLOSE_INTENT_DISCRIMINATOR }),
  );
}

export function getCloseIntentInstructionDataDecoder(): FixedSizeDecoder<CloseIntentInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
  ]);
}

export function getCloseIntentInstructionDataCodec(): FixedSizeCodec<
  CloseIntentInstructionDataArgs,
  CloseIntentInstructionData
> {
  return combineCodec(
    getCloseIntentInstructionDataEncoder(),
    getCloseIntentInstructionDataDecoder(),
  );
}

export type CloseIntentInput<
  TAccountIntent extends string = string,
  TAccountMaker extends string = string,
> = {
  intent: Address<TAccountIntent>;
  maker: TransactionSigner<TAccountMaker>;
};

export function getCloseIntentInstruction<
  TAccountIntent extends string,
  TAccountMaker extends string,
  TProgramAddress extends Address = typeof LAPLACE_PROGRAM_ADDRESS,
>(
  input: CloseIntentInput<TAccountIntent, TAccountMaker>,
  config?: { programAddress?: TProgramAddress },
): CloseIntentInstruction<TProgramAddress, TAccountIntent, TAccountMaker> {
  // Program address.
  const programAddress = config?.programAddress ?? LAPLACE_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    intent: { value: input.intent ?? null, isWritable: true },
    maker: { value: input.maker ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("intent", accounts.intent),
      getAccountMeta("maker", accounts.maker),
    ],
    data: getCloseIntentInstructionDataEncoder().encode({}),
    programAddress,
  } as CloseIntentInstruction<TProgramAddress, TAccountIntent, TAccountMaker>);
}

export type ParsedCloseIntentInstruction<
  TProgram extends string = typeof LAPLACE_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    intent: TAccountMetas[0];
    maker: TAccountMetas[1];
  };
  data: CloseIntentInstructionData;
};

export function parseCloseIntentInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedCloseIntentInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 2) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 2,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: { intent: getNextAccount(), maker: getNextAccount() },
    data: getCloseIntentInstructionDataDecoder().decode(instruction.data),
  };
}
